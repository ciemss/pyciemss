
#
# This file is automatically generated with 
# the System Biology Format Converter (http://sbfc.sourceforge.net/)
# from an SBML file.
#

#
# Model name = Munz2009 - Zombi Impulsive Killing
#
# is http://identifiers.org/biomodels.db/MODEL1008060000
# isDescribedBy http://identifiers.org/isbn/ISBN:1607413477
#

# some function definitions that are allowed in SBML but not valid in xpp
ceil(x)=flr(1+x)

@delay=50


# Compartment: id = env, name = environment, constant
par env=1.0

# Parameter:   id =  N, name = starting Population, constant
par N=500.0

# Parameter:   id =  p, name = birth rate, defined by a Rule

# Parameter:   id =  delta, name = delta, constant
par delta=1.0E-4

# Parameter:   id =  beta, name = beta, constant
par beta=0.0055

# Parameter:   id =  zeta, name = zeta, constant
par zeta=0.09

# Parameter:   id =  alpha, name = alpha, constant
par alpha=0.0075

# Parameter:   id =  n, name = number of kills
par n_1=0.0

# Parameter:   id =  k, name = kill ratio, constant
par k=0.25

# Parameter:   id =  tau, name = kill intervall, constant
par tau=2.5

# assignmentRule: variable = p
p=S*delta
aux p=p

# Reaction: id = birth

birth=p

# Reaction: id = death

death=delta*S

# Reaction: id = infection

infection=beta*S*Z

# Reaction: id = resurrection

resurrect=zeta*R

# Reaction: id = destruction

destructi=alpha*S*Z

# Species:   id = S, name = Susceptible, affected by kineticLaw

#WARNING : no initial state for this species
init S=0
dS/dt=( 1.0 * birth) + (-1.0 * death) + (-1.0 * infection)

# Species:   id = Z, name = Zombie, affected by kineticLaw
# Species is changed by 1 event(s)
init Z=1.0
dZ/dt=( 1.0 * infection) + ( 1.0 * resurrect) + (-1.0 * destructi)

# Species:   id = R, name = Removed, affected by kineticLaw

init R=0.0
dR/dt=( 1.0 * death) + (-1.0 * resurrect) + ( 1.0 * destructi)

# event : zombie_eradication
zombie_er=if ((t >= (n_1+1)*tau) and (k*(n_1+1) <= 1)) then (1.5) else (0.2)
global 1 {zombie_er - 1.1} {Z=Z*(1-k*(n_1+1));n=n_1+1}

@ meth=cvode, tol=1e-6, atol=1e-8
# @ maxstor=1e6
@ bound=40000, total=200
done

